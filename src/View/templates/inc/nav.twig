<nav id="navbar" class="navbar navbar-default navbar-fixed-top" >
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"  data-toggle="collapse" data-target="#mainNav" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><em>Ergo</em>Store</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="nav navbar-nav">
        <!-- <li class="active"><a href="#">Home</a></li> -->
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Catalog<span class="caret"></span></a>
          <ul class="dropdown-menu">
            {% for category in categories %}
              <li><a href="/catalog/{{category.uri}}">{{category.name}}</a></li>
            {% endfor %}
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Products<span class="caret"></span></a>
          <ul class="dropdown-menu">
              {% for section in sections %}
                <li><a href="/catalog/{{section.uri}}">{{section.name}}</a></li>
              {% endfor %}
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="input-group">
          <input type="text" class="form-control" placeholder="Search">
          <span class="input-group-btn">
            <button class="btn btn-success" type="button">
              <span class="glyphicon glyphicon-search"></span>
            </button>
          </span>
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
        {% if admin() %}
          <li><a href="/admin"><span class="glyphicon glyphicon-wrench"></span><span class="hidden-sm"> Back to Dashboard</span></a></li>
        {% else %}
          <li><a href="/account"><span class="glyphicon glyphicon-user"></span><span class="hidden-sm"> My Account</span></a></li>
        {% endif %}
        <li><a href="/cart"><span class="glyphicon glyphicon-shopping-cart"></span>
        {% if cart.size > 0 %}
          <span class="cart-badge-total">¥{{cart.total}}</span>
          <span class="badge cart-badge-products">{{cart.size}}</span>
        {% else %}
          <span class="hidden-sm cart-badge-total">Shopping Cart</span>
          <span class="badge cart-badge-products">0</span>
        {% endif %}
        </a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>
