{% extends 'base-admin.twig' %}

{% block title %}{{parent()}} | Users{% endblock %}

{% block scripts %}
    <script src="/js/dialogues.js"></script>
    <script src="/js/popover.js"></script>
    <script src="/js/check_all.js"></script>
    <script src="/js/jquery.tablesorter.min.js"></script>
    <script src="/js/tablesorter.js"></script>
{% endblock %}

{% block content %}
<div class="col-sm-9">
    <div class="well">
        <h2 class="management-header">User Management</h2>
        <div class="pull-right management-header-buttons">
            <a class="btn btn-primary" role="button" href="/admin/users/add"><span class="glyphicon glyphicon-plus"></span> <span class="hidden-xs hidden-sm">New user</span></a>
            <button type="button" class="btn btn-warning" data-toggle="modal" data-target="#resetDbModal" title="Reset database to initial state"><span class="glyphicon glyphicon-exclamation-sign"></span></button>
        </div>
        {% include "inc/flashes.twig" %}
    </div>
    <div class="well table-responsive">
        <form action="/admin/users/delete" method="post">
        <table id="tableToSort_5-7" class="table table-striped">
            <thead>
                <tr>
                    <th role="button">ID</th>
                    <th role="button">Username</th>
                    <th role="button">Email</th>
                    <th role="button">Registered at</th>
                    <th role="button">Type</th>
                    <th></th>
                    <th></th>
                    <th role="button" class="col-sm-1 checkable" style="text-align:center">
                        <input role="button" type="checkbox" id="checkAll">
                    </th>
                </tr>
            </thead>
            <tbody id="tableTBody">
                {% for user in users %}
                    {% include "admin/users/tr-user.twig" %}
                {% endfor %}
            </tbody>
            <tfoot>
                <td colspan="9" style="text-align:right">
                    <input class="btn btn-sm btn-danger" type="submit" name="deleteSelected" value="Delete selected">
                </td>
            </tfoot>
        </table>
        </form>
    </div>
</div>
{% endblock %}
